{{- $legacy := list "collaborationclusters.collaboration.re8ch.com" "collaborationrelationships.collaboration.re8ch.com" "publishedservices.collaboration.re8ch.com" "collaborationevents.collaboration.re8ch.com" -}}
{{- range $name := $legacy -}}
{{- if and (lookup "apiextensions.k8s.io/v1" "CustomResourceDefinition" "" $name) (not $.Values.migration.replaceCollaborationCRDs) -}}
{{- fail (printf "legacy CRD %s exists; export existing objects and set migration.replaceCollaborationCRDs=true for the destructive 0.2.0 replacement" $name) -}}
{{- end -}}
{{- end -}}
